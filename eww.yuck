; == == == This file creates all the EWW-windows == == ==

(include "./widgets.yuck")

; == == == Top bar == == ==
(defwidget left []
    (box :orientation "horizontal"
         :space-evenly false
         :spacing 15
         (NET_short)
         (CPU_short)
         (RAM_short)
         (battery)
    )
)

(defwidget middle []
    (box :orientation "horizontal"
         :space-evenly false
         :spacing 15
         :halign "center"
         (time)
    )
)

(defwidget right []
    (box :orientation "horizontal"
         :space-evenly false
         :spacing 15
         :halign "end"
         (media_control_short)
         (bluetooth_icon)
         (volume_bar)
         (brightness_bar)
         (system_short)
    )
)

(defwidget bar []
    (centerbox :orientation "horizontal"
               :class "bar"
               (left)
               (middle)
               (right)
    )
)

(defwindow bar
    :monitor 0
    :geometry (geometry :x "0%"
                        :y "1%"
                        :width "98.5%"
                        :height "2.5%"
                        :anchor "top center")
    :stacking "fg"
    :exclusive true
    (bar)
)

; == == == Bar expanding == == ==
(defvar date_reveal true)

(defwidget date_full []
    (revealer :transition "slidedown"
              :reveal date_reveal
              (box :orientation "vertical"
                   :space-evenly false
                   :spacing 5
                   (box :orientation "horizontal"
                        :space-evenly false
                        :spacing 5
                        (calendar :show-week-numbers true
                                  :day 10
                                  :class "calendar")
                        (box :orientation "vertical"
                             :space-evenly false
                             :spacing 10
                             :valign "center"
                             :class "clock-vert"
                             (hour)
                             (minute)
                        )
                   )
                   (weather_long)
              )
    )
)

(defwindow date_full
    :monitor 0
    :geometry (geometry :x "0%"
                        :y "3%"
                        :width "10%"
                        :height "10%"
                        :anchor "top center")
    :stacking "fg"
    :exclusive false
    (date_full)
)

; == == == Bottom bar/Workspaces == == ==
(defwindow workspaces
    :monitor 0
    :geometry (geometry :x "0%"
                        :y "0%"
                        :width "100%"
                        :height "0.75%"
                        :anchor "bottom center")
    :stacking "fg"
    :exclusive false
    (workspaces)
)
